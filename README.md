# palloc (pre12)
This is the older version of the repo, that has [subprojects](/subprojects/) compatible with compilers older than prog8c v12.0. If you don't care about compatibility with these compilers, just go back looking at the [`master`](https://github.com/adiee5/palloc/tree/master) branch.
